<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tender.team08.cs246.tender">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- To check the network status of the device to be able to use the Google API -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- To Read and Write to the local Calendar -->
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".Login_Activity" />
        <activity android:name=".AppointmentActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".PatientActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".AddPatientActivity" />
        <activity android:name=".DisplayPatientActivity" />
        <activity android:name=".DisplayTherapistActivity"></activity>

        <activity android:name=".EditPatientActivity"></activity>

    </application>

</manifest>